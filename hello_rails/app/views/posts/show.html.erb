<h1><%= @post.title %></h1>
<p><%= @post.body %></p>

<p>
  <%= link_to "Edit", edit_post_path(@post) %> |
  <%= button_to "Delete", post_path(@post), method: :delete, form_class: "as-link", data: { confirm: "Are you sure?" } %> |
  <%= link_to "Back", posts_path %>
</p>
